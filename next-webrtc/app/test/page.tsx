import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Test | Next App'
};

const page = () => {
  return <div>Test</div>;
};

export default page;
